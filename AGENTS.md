# BuildMate 开发约束

## 目标

BuildMate 把图纸、规范、模型和业务数据转换为有证据、可验证、可审批、可追溯、可回滚的工程决策。事实由确定性数据和引擎提供，LLM 只能提出判断。

## 环境与流程

- 开发环境以 Windows + PowerShell 为基准。
- 按 `Research → PRD → Tech Design → AGENTS.md → Build` 工作。
- 修改前阅读调用链、配置和测试；限制修改范围；优先复用现有实现。
- 采用小型、可测试、可回滚的垂直切片，不做无关重构。

## 强制不变量

- 所有业务调用必须携带 `tenant_id`、适用时的 `project_id`、操作者与 trace。
- Repository 强制租户/项目过滤，API 不拼接业务 SQL。
- LLM 结构化输出必须通过 Pydantic 校验；非法输出不得进入下一步。
- RAG 引用必须对应本次真实召回的 chunk；证据不足必须拒答。
- 几何、单位、坐标、规则、权限和事务由确定性代码控制。
- PDF/DWG 只在来源适配层分叉；下游 WallEvidence/WallModel 合同不得按图号或项目分叉。
- 图号、文件路径、图框、轴距和项目墙层名必须来自输入配置；OCR/YOLO/颜色不得决定最终墙坐标。
- 原图与 Revit 验收必须来自两份独立渲染，禁止从 WallModel 反绘原图作为验收依据。
- Revit 写入必须依次通过静态校验、Dry-run、持久化人工审批和 Bridge；失败必须回滚或明确失败。
- 外部 HTTP、LLM、向量库、队列和 Revit 调用必须有 timeout、有限 retry 和可观察失败。
- Agent 最多 20 步、12 次工具调用；任务使用至少一次投递和幂等消费。
- 禁止进程内字典作为任务、审批、模型版本或事件的唯一事实源。
- 禁止静默生成假数据、空 catch、关闭类型检查、删除测试或绕过权限。

## 完成条件

- 运行最相关测试；按风险运行全量测试、前端 build、Compose 配置校验。
- 检查 `git diff` 和 `git status`，保留用户已有修改，不提交 secret、运行时产物或调试文件。
- 没有运行的检查写明 `Not run`；失败如实记录。
