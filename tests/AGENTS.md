# Tests 约束

- 测试必须使用仓库 fixture 或确定性生成器，不依赖 `data/runtime`、个人下载目录或未提交文件。
- 每个租户级能力都要有跨租户拒绝测试。
- 外部依赖使用显式 fake/mock；失败和降级路径必须测试。
- 不修改业务断言来掩盖实现错误，不共享会造成顺序依赖的固定幂等键。
- Windows 下使用仓库内 `--basetemp`，避免系统临时目录权限污染。
