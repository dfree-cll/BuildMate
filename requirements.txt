# BuildMate Demo - 建筑行业智能助手
# Python 3.11+ 全异步

# 版本策略：轻量包用 >=（pip 自动解析），重量级模型包锁版本（避免构建时大升级下载）
# 锁版本后如遇依赖冲突，将对应行改回 >= 让 pip 解析
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.0
langgraph>=1.0
langgraph-checkpoint-sqlite>=2.0
langchain-core>=1.0
langchain-openai>=1.0
openai>=1.50
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
asyncpg>=0.29
pymilvus>=2.4
pydantic>=2.7
pydantic-settings>=2.3
PyYAML>=6.0  # 通用 PDF/DWG 墙体流水线的项目输入配置
python-dotenv>=1.0
PyJWT>=2.8
bcrypt>=4.0          # H4：用户密码哈希
redis>=5.0            # 企业化：多实例共享限流/吊销状态（REDIS_URL 未配置时内存回退）
aio-pika>=9.4         # 生产任务队列 RabbitMQ；本地数据库 Runner 不需要连接外部服务
boto3>=1.35           # 生产对象存储 S3/MinIO adapter；本地模式继续使用文件存储
prometheus-client>=0.21
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
alembic>=1.13         # 企业化：版本化数据库迁移
psycopg[binary]>=3.2  # langgraph PostgresSaver 依赖
langgraph-checkpoint-postgres>=2.0  # 企业化：检查点存 PG（多实例）
httpx>=0.27
numpy>=2.0,<3.0  # OpenCV 5 与 RapidOCR CPU 运行时要求 NumPy 2.x
opencv-python>=5,<6  # 图纸CV与RapidOCR共用，避免同时安装GUI/headless两套cv2
pillow>=10.0
python-docx>=1.1
rapidocr-onnxruntime>=1.4  # 标书 PDF/Word/图片 OCR
python-multipart>=0.0.9
pymupdf>=1.24  # PDF 解析（pdf_parser.py）
ifcopenshell>=0.8  # IFC BIM 模型解析（ifc_parser.py）
ezdxf>=1.4   # DXF/DWG 矢量解析（dxf_parser.py，第五期图纸感知通道）
shapely>=2.1  # 墙中心线拓扑成环与房间多边形审计
requests>=2.31
mcp>=1.9,<2              # MCP Server SDK（backend/mcp/ 两个独立服务；2.x 移除了 server.fastmcp，勿升）
duckduckgo-search>=6.0   # web_search_server 联网搜索
schedule>=1.2  # 可选：fetch_material_prices.py --daemon 定时更新用
# 本地嵌入/重排模型（锁版本，避免每次构建下载新版 torch 526MB+）
sentence-transformers==3.3.1
transformers==4.51.0
torch==2.5.1
torchvision==0.20.1  # 与 torch 2.5.1 配套，供 YOLO NMS 使用
ultralytics==8.4.115  # 本地图纸 YOLO 复核；生产使用前需确认其许可方案
psutil>=5.9  # transformers/accelerate 运行时进程与内存管理依赖
# 测试依赖也与运行依赖使用同一清单，保证本地、CI 与容器解析到同一版本范围。
pytest>=8.0
pytest-asyncio>=0.23
